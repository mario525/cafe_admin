<button type="submit"
    id="{{ $id }}"
    name="{{ $name }}"
    class="bg-{{ $color }}-500 text-white active:bg-{{ $color }}-600 font-bold uppercase text-xs px-4 py-2 rounded shadow hover:shadow-md hover:bg-{{ $color }}-400 outline-none focus:outline-none mr-1 ease-linear transition-all duration-150">
    {{ __($title) }}
</button>
